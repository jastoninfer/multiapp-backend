# will be DISCARDED once Spring boot application is ready to run in Docker container
DB_USER=${APP_DB_USER}
DB_PASSWORD=${APP_DB_PASSWORD}
DB_URL=jdbc:postgresql://localhost:5432/${APP_DB}
OIDC_ISSUER_URI=http://localhost:8080/realms/${KC_REALM_NAME}

DB_RESET=1